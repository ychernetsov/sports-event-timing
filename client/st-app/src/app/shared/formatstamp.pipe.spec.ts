import { FormatstampPipe } from './formatstamp.pipe';

describe('FormatstampPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatstampPipe();
    expect(pipe).toBeTruthy();
  });
});
